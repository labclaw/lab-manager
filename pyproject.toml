[project]
name = "lab-manager"
version = "0.1.0"
description = "OCR-first benchmark and ingestion foundation for lab consumables management"
requires-python = ">=3.12"
dependencies = [
  "openai>=2.0.0",
  "pillow>=11.2.1",
]

[dependency-groups]
dev = [
  "pytest>=8.4.0",
  "ruff>=0.11.0",
]
